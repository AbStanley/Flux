services:
  web:
    image: abstanley/flux-web:latest
    build:
      context: ./apps/client
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    restart: always
    networks:
      - app-network

  server:
    image: abstanley/flux-server:latest
    build:
      context: ./apps/server
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/flux_db
      - OLLAMA_HOST=http://host.docker.internal:11434
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=flux_db
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5433:5432"
    networks:
      - app-network

  caddy:
    image: abstanley/flux-caddy:latest
    build:
      context: .
      dockerfile: Dockerfile.caddy
    restart: always
    ports:
      - "${CADDY_PORT_PROD:-443}:443"
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data_prod:/data
    depends_on:
      - server
      - web
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data_prod:
    name: flux_production_postgres_data
  caddy_data_prod:
    name: flux_production_caddy_data
